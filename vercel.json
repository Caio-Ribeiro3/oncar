{
    "version": 2,
    "outputDirectory": "/api",
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}