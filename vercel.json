{
    "version": 2,
    "outputDirectory": "/server",
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}